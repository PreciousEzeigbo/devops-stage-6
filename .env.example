# Environment Configuration for DevOps Stage 6

# ===========================================
# DOMAIN & SSL CONFIGURATION
# ===========================================
# Your domain name (required for production)
DOMAIN=example.com

# Email for Let's Encrypt SSL certificates
ACME_EMAIL=admin@example.com

# ===========================================
# APPLICATION SECRETS
# ===========================================
# JWT Secret for authentication (use a strong random string)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secure-random-jwt-secret-here-change-this

# ===========================================
# TRAEFIK DASHBOARD CREDENTIALS
# ===========================================
# Generate htpasswd credentials with:
# echo $(htpasswd -nb admin yourpassword) | sed -e s/\\$/\\$\\$/g
# Note: $ must be escaped as $$ in docker-compose
TRAEFIK_DASHBOARD_CREDENTIALS=admin:$$apr1$$8gjhfgkj$$hgkfhgkhfgkhgkfhgk

# ===========================================
# REDIS CONFIGURATION
# ===========================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_CHANNEL=log_channel

# ===========================================
# ZIPKIN TRACING
# ===========================================
ZIPKIN_URL=http://zipkin:9411/api/v2/spans

# ===========================================
# SERVICE PORTS (Internal)
# ===========================================
AUTH_API_PORT=8081
TODO_API_PORT=8082
USERS_API_PORT=8080

# ===========================================
# INTER-SERVICE COMMUNICATION
# ===========================================
AUTH_API_ADDRESS=http://auth-api:8081
TODOS_API_ADDRESS=http://todos-api:8082
USERS_API_ADDRESS=http://users-api:8080

# ===========================================
# SPRING BOOT CONFIGURATION (Users API)
# ===========================================
SERVER_PORT=8080
SPRING_ZIPKIN_BASE_URL=http://zipkin:9411

# ===========================================
# NOTES
# ===========================================
# 1. Never commit this file with real values to Git
# 2. Use strong, randomly generated secrets
# 3. Keep different secrets for dev/staging/production
# 4. Rotate secrets regularly
# 5. Use environment-specific values
# 6. For production, consider using a secrets manager

# ===========================================
# TEST USER CREDENTIALS
# ===========================================
# Available test users (hardcoded in auth-api):
# Username: admin    | Password: Admin123
# Username: hng      | Password: HngTech
# Username: user     | Password: Password
